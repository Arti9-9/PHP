<header class="header">
    <nav class="navbar navbar-expand-lg navbar-dark bg-dark">
        <img src="{{ asset('uploads/img/library.jpg' ) }}" width="60" height="30" class="pt-5">
        <div class="collapse navbar-collapse justify-content-end" id="navbarNavDropdown">
            <ul class="navbar-nav justify-content-end col-6">
                {% if app.user %}
                    
                  
                        <a class="nav-link" href="{{ path('books_new') }}">Добавить книгу</a>
                   
                    
                        <a class="nav-link" href="{{ path('app_logout') }}">Выйти</a>
                  
                {% else %}
                   
                        <a class="nav-link" href="{{ path('app_login') }}">Войти</a>
                    
                {% endif %}
            </ul>
        </div>
    </nav>
</header>